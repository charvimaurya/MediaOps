prometheus-client
fastapi
uvicorn
psutil
pydantic
google-cloud-firestore
google-adk
google-genai
google-cloud-aiplatform
mcp
# Infra Agent's Grafana MCP path also needs the `mcp-grafana` server at runtime,
# fetched on demand via `uvx mcp-grafana@1.3.0` (not a pip dependency).
python-dotenv