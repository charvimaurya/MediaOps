# The ADK MCP integration imports modules from the 1.x MCP SDK.  MCP 2.x
# removed them, so keep this dependency within ADK's supported range.
google-adk[mcp]>=2.7.1,<3
mcp>=1.24,<2
google-genai
google-cloud-aiplatform[agent_engines,adk]
requests>=2.32.4,<3
python-dotenv
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
 
