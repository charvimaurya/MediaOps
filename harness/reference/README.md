# reference

Put the clean reference clip here, named `reference.mp4` (60-90s, visible
motion). `generate_variants.py` reads it from this folder — if the file has
a different common video extension (`.mov`, `.mkv`, `.avi`, `.m4v`) that
works too, as long as it's the only video file in this directory.
